## Copy to .env.local (gitignored) for local dev against the deployed Worker.
## The dashboard token itself is entered in-app (Settings page) and stored
## in localStorage, not here.
VITE_API_BASE_URL=https://framewrk.ashleyedwards305.workers.dev
